
    {{#each this}}
    <div class="comments-card-position">
        <div class="card post-card-size">
            <div class="card-body">
                <p class="card-text">{{comment_text}}</p>
            </div>
            <div class="card-body">
                <p class="card-text">posted by {{user.username}} on {{format_date created_at}} </p>
            </div>
        </div>
    </div>
    {{/each}}